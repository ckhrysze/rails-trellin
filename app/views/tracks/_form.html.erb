<%= form_for(@track) do |f| %>
  <% if @track.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@track.errors.count, "error") %> prohibited this track from being saved:</h2>

      <ul>
      <% @track.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :desc %><br />
    <%= f.text_field :desc %>
  </div>

  <%= f.select(:tier_1_id, options_for_abilities(@abilities) ) %>
  <%= f.select(:tier_2_id, options_for_abilities(@abilities) ) %>
  <%= f.select(:tier_3_id, options_for_abilities(@abilities) ) %>
  <%= f.select(:tier_4_id, options_for_abilities(@abilities) ) %>
  <%= f.select(:tier_5_id, options_for_abilities(@abilities) ) %>
  <%= f.select(:tier_6_id, options_for_abilities(@abilities) ) %>
  <%= f.select(:tier_7_id, options_for_abilities(@abilities) ) %>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
